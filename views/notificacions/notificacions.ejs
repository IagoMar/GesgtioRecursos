<!DOCTYPE html>
<html lang="ca">
<head>
    <meta charset="UTF-8">
    <title>Notificacions</title>
    <link rel="stylesheet" href="./styles.css">
</head>
<body>
    <main>
        <h1>Notificacions</h1>
        <ul class="list">
            <% data.notificacions.forEach(notificacio => { %>
                <li>
                    <strong><%= notificacio.estat %></strong> - 
                    <span><%= notificacio.fInici %></span> - 
                    <span><%= notificacio.fFinal %></span>
                    <a href="/notificacions/<%= notificacio.id %>">Detall</a>
                    <a href="/notificacions/put/<%= notificacio.id %>">Modificar</a>
                </li>
            <% }) %>
        </ul>
        <a href="/" class="btn">Volver a la Home</a>
    </main>
</body>
</html>