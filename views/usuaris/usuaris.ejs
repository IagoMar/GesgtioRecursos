<!DOCTYPE html>
<html lang="ca">
<head>
    <meta charset="UTF-8">
    <title>Usuaris</title>
    <link rel="stylesheet" href="./styles.css">
</head>
<body>
    <main>
        <h1>Usuaris</h1>
        <ul class="list">
            <% data.usuaris.forEach(usuari => { %>
                <li>
                    <strong><%= usuari.nom %></strong><br>
                    <span>Rol: <%= usuari.rol %></span><br>
                    <span>Contrasenya: <%= usuari.contrasenya %></span><br>
                    <a href="/usuaris/<%= usuari.id %>">Detall Usuaris</a>
                    <a href="/usuaris/put/<%= usuari.id %>">Modificar Usuario</a>
                </li>
            <% }) %>
        </ul>
        <a href="/" class="btn">Volver a la Home</a>
    </main>
</body>
</html>